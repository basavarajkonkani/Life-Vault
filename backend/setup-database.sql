-- Create database (run this as postgres superuser)
-- CREATE DATABASE lifevault;

-- Connect to lifevault database and run the following:

-- Create demo user (optional, for local development)
-- CREATE USER demo WITH PASSWORD 'demo';
-- GRANT ALL PRIVILEGES ON DATABASE lifevault TO demo;

-- The application will automatically create tables when it starts
-- Tables created: users, assets, nominees, vault_requests, trading_accounts
